o Divide cequiv/approx_star.v into several files, one for each non
  canonical operator so that they can be compiled in parallel.

o not not semantics.

o Move from using rule_true to rule_true3.

o Pointwise functionality vs. pairwise functionality.

o Currently, sequences are not allowed in the library: not soseq case
  in SOTerm's definition in terms/sovar.v
